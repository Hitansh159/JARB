import { useState } from "react";
// TODO: genrate error alerts 
const [errorOpen, setErrorOpen] = useState(false);


function genrateErrorAlert(text){
    setErrorOpen(true)
    return 
}

function closeErrorAlert(){

}